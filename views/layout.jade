doctype html
//This page is the layout template that all other pages extend from. It contains the navigation bar and footer
html
    head
        title= OpenHeart
        block headscripts
        link(rel='stylesheet', href='/stylesheets/style.css')
    body        
        nav
            div
                h1 Openheart
            ul
                li
                    a(href='//localhost:3000/') Home
                li
                    a(href='//localhost:3000/products') Products
                li
                    a(href='//localhost:3000/cart/checkout') My Cart
                li(style='float:right')
                    a(href='//localhost:3000/login')
                        if(typeof sess !== "undefined")
                            if(typeof sess.user !== "undefined")
                                | Logout #{sess.user}
                            else
                                | Login    
                        else
                            | Login
                  
        block content

